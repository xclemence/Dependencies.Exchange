<UserControl x:Class="Dependencies.Exchange.Graph.Views.SaveAssemblyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             MinWidth="600" Height="auto">
    <StackPanel Orientation="Vertical"  Margin="20 20 20 10" >
        <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
            <TextBlock Text="Assembly:" FontWeight="Bold"/>
            <TextBlock Text="{Binding Assembly.Name}" Margin="5 0"/>
        </StackPanel>
        <CheckBox Content="Include global assemblies" IsChecked="{Binding SaveGlobalAssembly}"/>
    </StackPanel>
</UserControl>
